import styled from "styled-components";

/**
 * Applied to every page of the site, this styled component identifies the main content for screen readers and creates a baseline for styling.
 */

export const StyledPageContainer = styled.main`
  margin: 0;
  padding: 0;
`;
