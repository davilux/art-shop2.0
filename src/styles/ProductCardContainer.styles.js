import styled from "styled-components";

export const StyledProductCardContainer = styled.ul`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  justify-content: space-evenly;

  list-style-type: none;
  padding: 0;
  margin: 0;
`;
